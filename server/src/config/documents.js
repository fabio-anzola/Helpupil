const documentTypes = {
  HOMEWORK: 'homework',
  EXAM: 'exam',
  TEST: 'test',
  REVISION: 'revision',
  SCRIPT: 'script',
};

const documentMimes = ['image/jpg', 'image/png', 'application/pdf'];

module.exports = {
  documentTypes,
  documentMimes,
};
